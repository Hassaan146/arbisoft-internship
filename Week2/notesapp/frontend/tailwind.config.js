/** @type {import('tailwindcss').Config} */
// Default Tailwind config, no extensions — per spec.
export default {
  content: ['./index.html', './src/**/*.{ts,tsx}'],
  theme: {
    extend: {},
  },
  plugins: [],
};

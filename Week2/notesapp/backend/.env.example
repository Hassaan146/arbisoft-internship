# Copy to .env and adjust. Never commit the real .env.
APP_NAME=Notes API
DATABASE_URL=sqlite:///./notes.db
# Comma-separated allowlist of frontend origins (no wildcard in prod).
CORS_ORIGINS=http://localhost:5173
